<%= turbo_frame_tag(dom_id(workspace)) do %>
<div class="is-flex is-justify-content-space-between is-align-items-center">
    <p class="title is-6 mb-0"><%= link_to workspace.name, workspace_items_path(workspace), target: "_top" %></p>
    <%= link_to t('shared.edit'), edit_user_workspace_path(workspace.user, workspace), class: "button" %>
</div>
<% end %>