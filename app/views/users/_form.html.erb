<% helper ||= false %>
<% tracking ||= false %>
<%= form_with(model: @user) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.label :email, class: 'label' %>
    <%= f.text_field :email, autofocus: true, placeholder: t('users.shared.email_placeholder'), class: 'input' %>
    <% if helper %>
      <p class="help">This is where we'll send you the magic link to log back in.</p>
    <% end %>
  </div>
  <% if tracking %>
    <div class="control"><%= f.submit t('users.new.submit'), class: 'button is-primary is-fullwidth', onclick: "_paq.push(['trackEvent', 'User', 'sign-up']);" %></div>
  <% else %>
    <div class="control"><%= f.submit t('users.new.submit'), class: 'button is-primary is-fullwidth' %></div>
  <% end %>
  
<% end %>
